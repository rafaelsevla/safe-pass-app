<!DOCTYPE html>
<html>
  <head>
    <title>SafePassApp Secure Authentication</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <div class="vh-100">
      <%= render(partial: 'shared/flash') %>
      <%= render(partial: 'shared/navbar') if account_page? %>
      <div class="h-100 container d-flex flex-column justify-content-center align-items-center">
        <% unless account_page? %>
          <%= logo %>
        <% end %>
        <div class="card card--auth shadow-sm w-100">
          <div class="card-body">
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
